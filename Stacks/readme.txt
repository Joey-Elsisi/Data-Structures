Assignment:

Implement a stack class (into files stack.h and stack.cpp). You can NOT use a standard library container class (list, vector, stack, etc.) for this.

Implement a simple postfix stack calculator for integers using your stack.